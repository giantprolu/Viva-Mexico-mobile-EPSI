name: vie_countdown
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1
  cupertino_icons: ^1.0.8
  flutter_launcher_icons: ^0.14.3
  audioplayers: ^6.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/doorClose.png
    - assets/doorOpen.png
    - assets/mexicain.png
    - assets/mexico.mp3

flutter_icons:
  android: true
  ios: true
  image_path: "assets/app.jpg"
